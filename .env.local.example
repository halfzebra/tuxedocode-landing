CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=
CONTENTFUL_PREVIEW_ACCESS_TOKEN=
CONTENTFUL_REVALIDATE_SECRET=
CONTENTFUL_ENVIRONMENT=master

# Contact form (app/api/contact) - SMTP submission credentials for the
# mailbox that both sends and receives contact-form messages.
SMTP_USERNAME=
SMTP_TOKEN=
SMTP_SERVER=
SMTP_PORT=